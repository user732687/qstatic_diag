pipeline:
  m: 100
  seed: 0
  p_min: 0.15
  p_max: 0.95
  gamma_min: 0.03
  gamma_max: 0.45
  outlier_sigma: 2.0
  efficiency_threshold: 0.10
  residual_low: 0.05
  residual_high: 0.80
  rho_thresh: 0.90
  bootstrap_n: 500
  alpha: 0.05
  num_seeds: 3
  device: cpu
  batch_size: 32

output:
  dir: ./results/resnet_demo
  formats: [json, csv, markdown]
  plots: true
